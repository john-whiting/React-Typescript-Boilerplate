import { FC } from 'react';
import { HomeContainer } from './StyledComponents';

const HomePage: FC = () => (
  <HomeContainer>
    <h1>
      This is a basic layout as a boilerplate to start a react project quickly.
    </h1>
  </HomeContainer>
);
export default HomePage;
