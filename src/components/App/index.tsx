import { FC } from 'react';
import AppWrapper from '../AppWrapper';
import RoutingPage from '../../pages';

const App: FC = () => (
  <AppWrapper>
    <RoutingPage />
  </AppWrapper>
);

export default App;
